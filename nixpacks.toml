# Nixpacks configuration for Railway deployment

# Tell Nixpacks to use Python provider
[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = [
    "pip install -e .",
    "pip install nicegui"
]

[start]
cmd = "cd web && python main.py"

[variables]
PYTHONUNBUFFERED = "1"
