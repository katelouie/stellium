# Stellium Web App - Railway Deployment
# Install the local stellium package and web dependencies

# Install stellium from local source (reads pyproject.toml)
-e .

# Web framework (3.x compatible - page functions are async)
nicegui>=3.0.0
