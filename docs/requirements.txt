# Capped <10: sphinx 10 removes APIs that sphinx-autodoc-typehints still uses
# (RemovedInSphinx10Warning at build time). Lift once that plugin is compatible.
sphinx>=9.0.4,<10
sphinx-rtd-theme
myst-parser
sphinx-autodoc-typehints
furo